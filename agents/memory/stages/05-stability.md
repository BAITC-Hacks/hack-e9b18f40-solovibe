# 05 Dedicated code review and stability
Status: not started. Requires completed04 suite. This is the requested single product-wide code review stage, not an automatic reviewer swarm or another planning ceremony. MAIN reviews actual integrated code and fixes confirmed defects. A bounded independent repair can be delegated with exact owned paths after MAIN establishes reproduction; no parallel speculative reviewers.

Entry: AGENTS/INDEX/STATE/PROJECT, PLAN C1–C9, this stage, relevant ENGINEERING/WORKFLOW and TESTING. Trace actual code paths, not planning claims. Reuse previous verification for unchanged code. Full UI/browser testing stays user-owned; no routine screenshots/Playwright. Core defects block06.

## 05.1 Trace real workflows and repair confirmed failures
Outcome: all critical promises are checked at their real boundaries; reproducible defects are fixed with focused evidence.
Paths: actual src/features/city/,src/server/city/,API routes,db/schema/migrations,worker/runtime/storage/deploy integration; only files justified by findings. MAIN owns shared changes and this stage's compact findings rows.
Inputs/results: V1–V8 existing fixtures, real saved example/AI artifact/brief/export/share; representative second owner and injected failure boundaries. No invented provider success or deletion of non-test data.
1. Follow V3 from public start through guest identity, authoritative calculation, run creation, model tools, candidate save and reopened UI. Confirm user input actually changes tool parameters/results; a canned branch or LLM text-only recommendation is a defect. Inspect source/version/evidence check before completed.
2. Review deterministic model and search: clipping after summed effects, unscaled synergy, strict threshold, exact5 vs draft, city/district scope, same-direction limits/exclusions, deterministic ties, cancellation, exhausted vs interrupted certificate, pinned targets/objective/assumption cache keys. Verify criterion5 with a meaningful changed input, not the false assertion that every selection change must change Score.
3. Review nonlinearity/explanation: attribution sums, minimum district ties, evidence freshness, score/constraint differences, infeasible explanations and repaired stress evaluated under same assumptions. No real-life certainty/inhabitant count is inferred from synthetic values. Use V4 and M11/Almaty boundary to probe misleading success.
4. Trace writes and races: guest token/claim vs active run, same client mutation retry, active revision CAS, late run result, deleted scenario/run cancellation, worker lease loss after provider call, receipt commit before crash, duplicate cleanup/export job, concurrent brief edits and public snapshot. A lease-fenced stale worker must not mutate after takeover.
5. Trace permissions with two distinct principals/accounts across every actual route/tool: scenario read/rename/delete, branch/apply, search/run/events/cancel, brief read/update, file preview/download/delete, guest claim, share creation/revoke. Check same-origin mutations, body/field validation, no owner chosen by model/client, ID-based404 and account-cache isolation. Public snapshot only reveals its intended frozen projection.
6. Inspect storage/export boundary: metadata ready iff bytes valid; pending recovery/tombstones, backend identity, HTML escaping/CSP, CSV formula safety, no arbitrary URL fetch, exact source text/data, private/local directory protection, token leakage/cache/referrer, immediate revocation on new downloads. Verify guessed file key doesn't bypass rights.
7. Inspect durable jobs and cost: missing key remains action-local; all state transitions terminal/pending honestly shown; provider429/timeout/schema failure bounded; cancel prevents new effects; heartbeat/recovery/global limits actually enforced. No uncapped raw catalogue/history expansion or secret logging.
8. Inspect startup/deployment: schema+seed applied on fresh DB, app+worker same revision, persistent local volume and private R2 paths, no agents/ runtime dependency, missing provider config not a crash loop, no worker omitted by deploy/rollback. Read ops only if correcting delivery. Preserve migration ledger/volumes and unrelated services.
9. Check TESTING's current entries once at this boundary; distinguish real defects from feature work never completed. Reproduce minimum case and repair the narrow cause. Add focused regression only for substantive behavior, not cosmetic changes. Do not tick until verified; preserve original user text/concurrent additions.

Use these rows for confirmed findings only; replace placeholder when facts exist. No separate report system:
| Path / defect | Minimal reproduction | Fix / focused evidence | State |
| --- | --- | --- | --- |
| No review executed yet | — | — | Pending |

Block inherits all C3/C4/C7 behavior/rights/localization. A repair changes service and all affected UI states/translations together; no log-only error handling. Independent repairs may be delegated by whole-file ownership while MAIN fixes shared boundary. Writers finish before checkpoint.
Acceptance: each confirmed core defect has a reproduced before-case and passing after-case; unresolved core problems block block completion. V1–V8 evidence covers actual promises, not only schema/build. Preserve harmless limitations honestly.
Commit: one coherent integrated repair package (or focused separate repair commit when justified). If no defect requires code edits, record actual reviewed scope/evidence in existing memory, not an empty code commit.

## 05.2 Release verification and reproducibility gate
Outcome: stable product is ready for truthful README/media, with operational and first-session evidence.
Paths: focused checks/runtime and any confirmed final fixes; PROJECT/STATE and this findings table. Final README still untouched.
1. Re-run only checks affected by05.1 fixes, then final type/lint/build through existing checkpoint as appropriate. Execute the weakest connected revision chain V5 against actual DB: selected plan→stress→repair→manual brief edit→refresh→export→reopen/share. Check current vs historical source labels, preserved user text and correct numerical source.
2. Complete V6 interruption/missing-key/429/quality failure/cancel/lease recovery assertions and V7 two-owner/local/R2/revoke assertions if not already valid after changes. Real provider check must hit actual adapter; mock failures establish recovery only. If R2 credentials unavailable, label R2 unverified and complete local path; if required live AI unavailable, record external blocker, never report full product completion.
3. Repeat first-session timing only if changes affected it or prior result failed. Report actual time/environment/user effort and useful resulting artifact, not a claim of universal latency. User observes original identity/first-viewport transformation/accessibility; fix actual feedback without introducing automated visual audit loops.
4. Prepare V9 on an isolated temporary checkout/export directory and separate Compose project/volumes/ports so the running user's app/data are untouched. Include only repository-required runtime material, not agents/ private setup. Safe env from env.example/setup; run the ONE primary pnpm local:up command after env setup. DB, migration/seed, app and worker must become healthy; source example/save/file persistence work without OpenAI/R2. Then configure key and verify bounded live AI if needed. Clean only verified test-owned resources, never shared volumes.
5. Check deployed public health exact SHA AND matching worker heartbeat; actual event stream through HTTPS is timely/resumable, secure session works and export/link works. Reuse existing manual-deploy after checkpoint, no second push/deploy ceremony. Record actual live revision or deployment blocker precisely.
6. Summarize verified capabilities/limitations/commands in PROJECT, not README drafts. Stage findings are resolved or explicit noncore limitation with impact; no unsupported blanket claim of zero bugs. STATE records06.1 as next action, actual check scope/commit/push/deploy status and any retained server role.

Acceptance: complete approved suite remains connected; V1–V9 have relevant successful evidence or a precise external blocker that prevents marking this gate complete. Confirmed defects that break core promise, privacy, persistence or reproducibility are fixed before06. Conditional storage integrations are described accurately. No unresolved implementation placeholder may be promoted into final marketing.
Checkpoint: final integrated stability package and verified deployed revision; clean tree/no active file writers. Handoff06.1 to write README from verified implementation before video.
