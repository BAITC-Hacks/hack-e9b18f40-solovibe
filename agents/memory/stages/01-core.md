# 01 Core and first real AI result
Status: not started. MAIN completes01.1–01.3 in this chat and hands off to02. No implementation of later stage features as a substitute for this boundary.

## Entry and exit
Read root AGENTS, INDEX, STATE, PROJECT, PLAN C1–C9, STACK; relevant ENGINEERING, integrations AI/DATABASE/AUTH, HARNESS and design SYSTEM/DIRECTION/UX/COPY. Source: knowledge/cases/akim-dataset.md and verified HACKATHON examples. Reuse prepared files and dependencies.
Entry housekeeping: reconcile known planning changes and checkpoint the finished agents/ planning package first as specified in PLAN. Verify Node meets STACK before pnpm. Do not reinstall/recreate working foundation, regenerate env, reset database or change origin.
Exit: a guest/account can open the real example, edit/save valid decisions, submit a goal, observe real tool work, receive/reopen a checked saved alternative. Exact engine/search, basic identity/primitives and durable execution are shared contracts for02. Brief/export/share/advanced comparison/stress remain later work, not falsely exposed as complete.

All blocks inherit C1–C7 rights, ru/kk/en, no-fake-success, CAS and recovery. MAIN owns migrations/types/routes/runtime/dependencies/messages. First independent assignment can own UI primitives/logo components after tokens/contracts are fixed while MAIN builds domain/services; no separate data/provider adapter.

## 01.1 Exact city rules, constrained search and visual primitives
Outcome: every later interface/tool consumes the same verifiable model; real seed/reference result can render with original identity.
Paths: src/features/city/{contracts.ts,data/akim-v1.ts,engine.ts,search.ts,evidence.ts,__tests__/}; data/akim-dataset.md; src/server/db/schema.ts,drizzle/,runtime/migrate.ts; src/components/ui/,src/components/brand/; src/app/globals.css; messages/*.json. New data/runtime files must work without agents/.
Entities: immutable dataset/rules/evaluator IDs; typed Decision, Constraints, Evaluation, SearchResult, EvidenceRef; city_datasets seed table.
1. Transcribe/canonicalize all50 values, population shares,14 measures, lag,weights,exclusions/synergies exactly. Verify population/indicator weights sum1, district totals match source. Seed idempotently after migrations under existing startup locking; no provider calls/build-time DB requirement.
2. Implement pure validator and evaluator C1 with typed issue IDs/params; calculate final indicators before all critical checks. Separate draft, official and baseline-reference. Use stable rational/integer arithmetic where straightforward or precision-safe numbers with documented epsilon solely for equality tests, never rounding the<40 rule.
3. Implement exhaustive measure-combination/target enumeration with early pruning, deterministic ranking and C2 constraints. Count actually evaluated/valid plans; support cancellation/deadline checks. Run CPU-heavy search away from request/UI event loop (worker thread/process service) or use bounded yielding; use immutable cached baseline result only after exact computation. Cache max64 input entries per process with bounded payloads; never infer authorization from a cache hit. Return top3 distinct results, not variations of a saved label.
4. Return source-backed evidence graph: indicator base, measure realized effects, synergy, clipping, district sum, population weight, critical penalty and total. Tool/UI references use stable typed IDs, not model URLs.
5. Define original semantic tokens, font, accessible Button/Input/Select/Dialog/Sheet/Tabs/Alert/SaveState/LanguageSelector and logo component from approved sketch. Keep proposal geometry original; verify 24px silhouette/monochrome by construction, no compulsory artwork render. Catalogue direction colors remain accessible words/icons, not color-only.
6. Add focused domain/search commands and shared fixtures V1/V2. MAIN adds translation namespaces and localized data labels once; all later workers consume them.
Acceptance: V1 source example cost95/56.543070, baseline52.557680, full50 values/evidence; exact40, M11/Almaty38.25, clipping, lag8, synergy and exclusions; drafts have no official Score. V2 exhaustive694395/best57.236735 and all-five68200/best56.344510. Test order invariance, source immutable hash, invalid targets and third same-direction choice. Benchmark real search latency/memory; do not accept a hardcoded optimum fixture as search. Focused `pnpm test:domain` plus checkpoint static/build checks.
Checkpoint: one complete domain/data/primitives package. UI foundation can be handed over before MAIN's build; no concurrent global-file writers.

## 01.2 Owned city board and durable revisions
Outcome: first substantial manual result, saved anonymously and claimable by an account, with instant valid editing.
Paths: src/server/city/{owners.ts,scenarios.ts,revisions.ts}; src/features/city/components/{CityBoard,DecisionTray,Catalogue,ScenarioShell}*; src/features/city/client/*; src/app/[locale]/{page.tsx,city/[id]/page.tsx,scenarios/page.tsx,sign-in/,sign-up/}; src/app/api/city/{scenarios/,owners/claim/}; src/lib/auth-client.ts,src/server/auth.ts; schema/migrations/messages.
Entities: city_owners/scenarios/revisions/evaluations, constraints stored on revision; owner-indexed title/currentRevision/version/deletedAt, unique clientMutationId under owner.
1. Implement server principal resolution/guest cookie/account claim C3. Guest creation only from an intentional POST. Claim resolves authenticated session and matching guest cookie, rebinds owner under transaction, revokes token and clears it. Do not expose unconfigured email delivery/password-reset success.
2. POST example creates a source-example revision and exact evaluation; blank creates an incomplete saved draft. Root until04 is a small functional entry with brand/start/selector, not a placeholder marketing page. Keep selected source/intent across authentication. Library create/rename/fork/delete/search title (bounded)/pagination and honest empty state; deletion soft-revokes access immediately.
3. Build desktop city board and accessible mobile district list: district name, main needs, before/after values, selected measures and budget. No real geography claim. Five-slot tray; catalogue shows cost, applicable target, direction and lag. Add/replace/remove atomically; do not require temporarily saving6 measures or an over-budget selection. Invalid attempt shows reason and legal next action. All five directions remain selectable under primary dataset rules.
4. Pure client preview computes instant changes, server re-evaluates and persists revision/evidence in one transaction with expectedRevisionId. Debounce settled edits around400ms with last-write queue/flush on safe navigation; title/inputs preserve failure state. First active save only after ack; retries reuse idempotency. Keep preview distinct from authoritative saved result.
5. Feed canonical replies into one shared state so budget, district values, tray, library and result update together. Add invalidation handling for another tab and CAS conflict recovery: compare current server vs local draft, never automatic destructive replacement. Locale switch uses same scenario and retains unsaved edit queue safely.
6. Add owner/CAS/domain integration checks with real PostgreSQL. Keep tests isolated per generated test owner; delete only those records.
Acceptance: create example without signup, immediately see95/56.54 and0 critical pairs; valid change produces different server result, reload retains it; switching ru/kk/en keeps inputs. Over-budget/conflicting requests sent directly to API cannot create active revisions. Two guests denied cross-access, duplicate POST does not duplicate scenario, old revision fails409 preserving new selection, sign-up/sign-in claim retains scenario. Manual use survives absent OpenAI key. `pnpm test:integration` covers actual DB saves/permissions, not UI screenshots.
Checkpoint: complete owned board/auth/library package. TESTING checks may begin at natural integration boundaries after this usable UI exists.

## 01.3 Live adaptive AI loop and durable worker
Outcome: a real user goal reaches tools, checked calculated alternative and durable usable result early, before later presentation work.
Paths: src/server/city/ai/{context.ts,tools.ts,quality.ts,prompts/*,runner.ts}; src/server/city/{jobs.ts,events.ts,limits.ts}; runtime/{city-worker.ts,worker-health.ts}; src/features/city/components/{AssistantPanel,RunStatus,AlternativeCard}*; src/app/api/city/{runs/,searches/}; Dockerfile,compose.yaml,compose.production.yaml,package.json,.env.example; agents/scripts/manual-deploy.mjs,deploy-ssh.sh and ops/SETUP.md only for worker delivery changes; schema/migrations/messages.
Entities: city_jobs common queue, runs,run_events,tool_receipts,analyses,search records,candidate branch revisions,worker heartbeat/rate windows. Statuses queued/running/waiting_input/completed/failed/cancelled; partial artifacts remain accessible. waiting_input ends lease; corrected intent creates a child run bound to current chosen source. No generic chat platform is required: objective field + editable prior requests + saved operation history + structured result cards suffice.

### Execution and transport contract
1. POST /runs validates ownership/current input/config/rate before queue. Input={scenarioId,inputRevisionId,procedure:plan|explain,objective<=2000chars,clientRequestId}. ACK202 returns durable run ID; same owner/request ID returns same run. Explicit user text appears immediately. After valid ownership/input, missing configuration persists the request as a failed attempt with AI_UNAVAILABLE and no queued job/provider call, returning its ID with the error so the text survives reopen. A failed save is never labelled persisted. Rate-rejected input stays in the existing bounded draft/attempt rather than creating unlimited failure rows.
2. Worker claims due city_jobs record in short transaction using SKIP LOCKED;30s lease, heartbeat10s, monotonic lease/fencing token. Maximum2 processing slots deployment-wide,1/owner. Expired lease recovery max2 attempts within the120s run deadline; save outputs/usage between steps. Mutations check current lease token AND cancellation/deletion before transaction commit. No DB transaction held across provider request. Model tools call shared search/export/domain services directly, never await a nested queued job competing for these same slots.
3. Persist ordered observations and tool receipt keyed(runId,logicalStepId,argumentHash) before next model step. If worker dies after committed mutation, resume returns same result reference. Different args cannot reuse a receipt. Resume includes saved observation summary and source, not an invented completed transcript. Cancellation aborts active provider/search and prevents further writes except terminal state/retained successful artifacts.
4. Run AI SDK tool-calling loop with prepared OpenAI adapter; use installed types for streamText/generateText structured output. Feed actual results into the next model decision. Bound steps/tokens/time per C5. No model call if key absent. Give original source plus user constraints precedence over model suggestions.
5. Event stream GET /runs/:id/events uses authenticated SSE with ordered seq/Last-Event-ID or after cursor; replay saved events after reconnect, deduplicate. It reports real tool activity/candidate/result IDs; partial text is explicitly unfinished. Client can also fetch run snapshot after disconnect. Stop UI cancels run, closing a page does not. Event status never invents thinking/progress percentages.
6. Runtime integration: same image separate worker service, build a Node worker bundle and worker health helper, configure local and production Compose with shared DB/env/file volume. Prepared ai/db/storage imports contain server-only guards; extract minimal reusable runtime cores/factories behind existing guarded Next exports, or use a verified build adapter that safely strips only this marker in the Node-only bundle. Do not import Next request APIs into worker or expose secrets to client.
7. Add worker to pnpm local:up/start path and developer worker command; no second manual action for jury. Update both deploy scripts including rollback to replace/verify app AND worker from identical SHA after migration. Worker heartbeat includes revision; health/readiness distinguishes missing AI config (manual app still healthy) from dead worker. Worker uses configured DB and can remain alive without key.
8. At checkpoint verify public streaming/replay through existing unbuffered proxy, cookies, exact app+worker revision. Read ops SETUP for these delivery changes, preserve unrelated services.

### Initial tools (same services as UI)
| Name | Validated input / output | Authority and use |
| --- | --- | --- |
| readScenario | owned scenarioId,revisionId → dataset/rules,decisions,constraints,evaluation,evidence refs | Read only; exact immutable source |
| readEvidence | evaluationId,known refs<=20 → numeric values/provenance | Read only; owner/derived dataset scope |
| validatePlan | decisions + known source → valid/issues/cost | No score for invalid final set |
| simulatePlan | source + valid5 decisions → checked evaluation/evidence | Calculate, not persist an active plan |
| searchPlans | C2 constraints/source,limit<=3 → actual candidates/certificate or conflicts | Bounded owned search; cannot change rules |
| saveAlternative | sourceRevisionId,checked candidate/search IDs,title<=120 → branch/evaluation IDs | Idempotent save, owner from run; no caller owner; never current-plan overwrite |
| saveAnalysis | source IDs + typed conclusion blocks/evidence refs → saved analysis ID | Domain quality must pass; no unverified text-only success |

Every tool has name/purpose, strict schema, scope, actual implementation, bounded output, retry/cancel semantics. Failures return structured issues the model can use to correct a proposal. Search/compute observations are compact; never send694k candidates to model.

### Subject-specific procedures and quality
Create separate maintainable modules, not one general role paragraph:
- **Construct/repair**: source=current user's revision; preserve locked ID+target and hard conditions; normalize intent to supported chips; check conflicts first; search legal complete portfolios; inspect at most3; choose by requested objective and explain an actual tradeoff; save only evaluated branches. For «Сохрани школу в Нуре, улучши результат» encode locked M7/Nura before search, never replace with M9 or move school. If ambiguity materially changes a constraint, return one waiting_input question; otherwise use visible defaults.
- **Explain**: read the exact evaluation/measure evidence; distinguish weighted average, weakest district and threshold penalty; show at least one concrete benefit, tradeoff/risk and model limitation where applicable. Do not force a negative effect if none exists. For source example, reference Nura S1=48,S2=43.75,Ncrit0 and synergy B1; never infer real inhabitants served, tenge or predictive confidence from synthetic inputs.
- **Comparison preparation**: initial plan result includes source/candidate comparison facts and changed/unchanged decisions; no preference claim from title alone. Full multi-plan UI follows02.
- Output schema: blocks with kind benefit|tradeoff|risk|limitation|infeasible|noImprovement, sourceRefs and brief prose; typed numeric references supply numbers/units on render. Reject unknown/stale evidence, invalid candidate, lost lock, unsupported all-directions/zero-critical claim, conclusion direction inconsistent with linked delta. Narrative contains no new numeric claims; descriptive assumptions are labelled. Score isn't evidence of real economic gain.
- Completion requires saved legal5-measure branch + matching evaluation + evidence-supported explanation; or verified infeasibility + actionable explicit condition change, with original untouched. One bounded quality repair; if still wrong preserve calculations/candidates and show failed explanation with retry. Never cache canned LLM narrative.

### Acceptance and checkpoint
Run V3 with actual configured model: source example→request preserving M7/Nura→tool observations→legal saved alternative→read/reopen same IDs. Validate goal/lock/evidence, not exact prose. Run V4 through domain search now; live model must observe infeasibility, not invent a candidate. Record a compact real execution proof in PROJECT (IDs can be test-local), no keys/full prompts.
Test missing key, cancellation, restart after save receipt, retry/duplicate POST, stale source while generation runs, and cross-owner tool IDs. Stubbed failure injection is allowed to verify branches but not claimed as live-provider success. Without a usable key, complete all independent work, report exact blocker and do not mark live AI acceptance achieved.
Introduce `pnpm verify:ai` for bounded live path and worker-focused checks. Integrated checkpoint after all writers finish. Handoff02 reads this implemented contract through code and PROJECT; it does not re-probe successful unchanged provider wiring.
