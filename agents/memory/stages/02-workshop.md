# 02 Complete decision workshop
Status: not started. Requires completed01 and its actual code/contracts/evidence; do not recreate foundations. This chat finishes02.1–02.3 and hands off to03.
Read PROJECT, PLAN C1–C9, own stage, relevant design/AI guidance. Existing city services/types are authoritative. Inherit C3/C4 owner/CAS/idempotency, C7 ru/kk/en/responsive states, C5 run limits. MAIN owns routes/types/schema/messages; a worker may own evidence views while MAIN implements search comparison, then stress views after contract handoff. No concurrent writers to shared files.

## 02.1 Evidence that explains each consequence
Outcome: user can understand and change the five choices, locate a weak indicator, inspect the formula and see the actual source of the change.
Paths: src/features/city/{evidence.ts,attribution.ts,components/evidence/,components/CityBoard*,components/Catalogue*,components/DecisionTray*}; src/server/city/scenarios.ts; messages/*.json; domain fixtures.
Inputs/results: current saved revision plus local preview, selected district/measure; no new external data. Evaluation retains all50 values, active synergies, clipping and reference baseline.
1. Finish catalogue interaction: direction/filter/search, district target, selected/locked/excluded states, cost, lag, full vs realized effect, relevant conflict reason and replacement action. Keyboard and touch both reach every action. Atomic replacements preserve the5-choice limit. Budget is a stable primary control/status, not a decoration.
2. Finish district drilldown: ten indicators, before/after, threshold crossing, actual measure contributions and constraints. Formula disclosure decomposes0.7 weighted mean +0.3 weakest district -critical count. Tie weakest districts explicitly; avoid saying an unaffected district improved. Display units as model points/virtual budget, never population counts or currency.
3. Implement exact five-measure Shapley allocation of model Score change (32 subsets) if presenting additive per-measure Score contributions. Subsets use the mathematical evaluator without the submission count requirement; they are diagnostic, not valid scenarios. Account for synergies, clipping, weakest-district switches and threshold penalties. Sum contributions equals final-minus-baseline within numeric tolerance. Label «Вклад в модели», explain it averages contribution across orders and does not prove real causality. Direct indicator effects remain separately visible. Do not substitute leave-one-out differences and falsely sum them.
4. Explain eight-quarter horizon and realized lag fraction; show full and realized effect without inventing an annual/quarterly progress chart. Surface M11 congestion penalty and near-threshold issues at selection, not only deep in AI prose.
5. Add fast local preview/authoritative reconciliation and accessible announcements on meaningful changed totals. Hover enriches but never hides required evidence. Comparison/assistant can link directly to indicator evidence; new revision updates links/version state.
6. Extend explain procedure with attribution tool `getAttribution({revisionId})→{baseline,final,perMeasure,evidenceRefs,method}`; strict ownership/source checks and cancellation. Model may explain this result but cannot invent a causal percentage.
States: no measure/empty district selections explain next action; preview vs saved distinguishable; stale fetched evidence is discarded or labelled with its old source. Human error actions target conflicting measures.
Acceptance: V1 source trace reaches exact source values/measure effects; Shapley sum and a threshold/synergy case verify nonlinearity. Keyboard-equivalent data/actions have localized names; longer kk labels can wrap. Domain focused checks plus checkpoint; no fresh global review/browser loop.
Commit: complete catalogue/evidence/attribution capability.

## 02.2 User priorities, branches and comparisons
Outcome: users can obtain and act on alternatives without re-entering context; the visible price of their condition has exact evidence.
Paths: src/features/city/components/{Priorities,Comparison,AlternativeCard,RevisionHistory}*; src/server/city/{searches.ts,comparisons.ts,revisions.ts}; src/server/city/ai/{tools.ts,prompts/compare.ts,prompts/plan.ts}; /api/city/{searches,comparisons,scenarios/[id]/forks,scenarios/[id]/apply}; messages/schema as needed.
Inputs/results: C2 normalized constraints plus saved source revision; branches already exist from01. Add derived comparison response referencing up to3 owned revision IDs rather than duplicating scenario state.
1. Build goal shortcuts and editable condition chips: lock a measure/target, exclude a measure, direction coverage, direct-district count, critical cap, district/indicator floor, budget ceiling and objective. Show precise distinction between direct investments and city-wide benefits. Basic controls stay simple; floors/alternative objectives live in expanded priorities.
2. Search POST returns durable search ID/status and bounded actual progress; run service on prepared worker infra for long enumeration. Reuse full input hashes/certificates and invalidate on any effective input change. Background search payload includes owner/source/cancel handling, independent of an LLM when user uses deterministic controls. Provider-free exact search is a valid user action.
3. Candidate cards show changed and retained decisions, cost, Score, weakest district, critical pairs and fulfilled/failed condition chips. Sort by actual requested objective; no forced recommendation if tied. Each card can inspect, save/fork or apply. Apply sends expected active revision/source candidate IDs; it creates a new revision and invalidates dependent analysis, not a text-only suggestion.
4. Comparison endpoint `POST /comparisons {revisionIds:[1..3],assumptionId?}` validates owner and dataset/rules equality; returns decision diffs and evaluation matrix with source refs. Standard view excludes sensitivity mixing. Keep selected IDs in scenario/view state and reuse saved branches on reopen; no model is needed just to draw a table. Team snapshots use separate stage03 authority, never grant private source access.
5. Priority-price operation runs before/after exact search with all other conditions/objective fixed. Persist search/certificate refs and changed-condition description; return cost difference, Score difference and actual measures/district consequences. For non-maxScore objective call it a comparison, not a global optimality gap.
6. Infeasibility UI names checked conflicts and offers explicit actionable edits without discarding the goal. V4 all-five+zero-crit must explain M8 plus M7/M9 requires two social measures; no fabricated feasible plan. Search for at most3 single-condition relaxations only when useful and bounded; label each actual relaxation. Do not promise universal minimal unsatisfiable cores.
7. Complete history: old revisions, source labels, branch title, restore-as-new, rename/fork, and stale-source comparison. Pinning/changing target updates all dependent constraints. Conflict409 preserves both the local request and current server choice.
8. Add tools `comparePlans({revisionIds})`, `applyAlternative({scenarioId,expectedRevisionId,candidateRevisionId})` (only if run intent explicitly authorizes editing current scenario), and `priceCondition({sourceRevisionId,baseConstraints,changedConstraint})`. Agent context receives active constraints and selected comparison; never trusts caller-supplied assistant observations.

### Compare/priority prompt contract
Task: answer which candidate better meets THIS user's conditions, not which has the most impressive label. Read all referenced evaluations; align dataset, assumptions and locked decisions; compare code-returned deltas; name a concrete beneficiary and concession when present; save comparison analysis referencing actual IDs. Return noImprovement/tie honestly.
Example: minDirectDistricts1→2 under maxScore/default rules yields98→100 cost,57.236735→56.997700 Score and a move to two districts. It must not say the score increased or all five districts received direct investments. With a locked school, rerun both comparison sides under that lock rather than reuse the global0.239035 gap.
User changes are already authorized by clear wording such as «Замени текущий план этим вариантом»; do not add repetitive confirmation for ordinary reversible edits. A request «покажи варианты» never changes current plan.

States/locale: show actual search queued/running/cancelled/incomplete/complete; existing board remains usable. Changing constraints marks prior candidates as from earlier conditions. Readable localized condition chips/error explanations; tables collapse to aligned per-metric comparisons on mobile, not horizontal-only access.
Acceptance: V2/V4 plus changed lock changes candidate/hash; applying branch updates board/tray/library without reload, rollback/restore yields new revision; stale apply409 does not overwrite. Test same-score candidates, partial search no-optimum claim, infeasible response source and non-owner compare. Execute live compare procedure only if new prompt/tool behavior needs validation, reusing unchanged provider proof.
Commit: integrated constraints/search/history/comparison.

## 02.3 Sensitivity experiments and repair
Outcome: test a chosen cost/delay risk and obtain a feasible repaired portfolio under the SAME assumptions; save/revisit it with baseline intact.
Paths: src/features/city/{stress.ts,components/stress/}; src/server/city/{stress.ts,ai/prompts/stress.ts,ai/tools.ts}; /api/city/stress-tests/; schema/migrations/messages; shared fixtures.
Entities: city_stress_tests {id,ownerId,sourceRevisionId,assumption,baselineEvaluationId,stressedEvaluationId,status,repairRevisionIds}; evaluations kind=sensitivity. Reuse run/search/branch services, do not create another calculator.
1. POST /stress-tests {sourceRevisionId,measureId,costIncreasePct,extraLagQuarters,clientMutationId}; schema validates bounded values and initially selected measure. Compute cost in hundredths, lag cap8; preserve fixed synergy semantics. Save source and assumption/version.
2. UI offers cost increase and delay as explicit user-set conditions, with input units, bounds and reset. Baseline and experiment always labelled; default is no fabricated random event or probability. Show resulting feasibility and indicator effects; over-budget scenario clearly lacks valid Score. User can still inspect why it failed and request repair.
3. Repair search carries original user commitments, same100 budget, source dataset and identical override. Locked affected measure cannot silently be dropped. Unlocked measure can be replaced; state why that evades the selected risk. Returned candidates are saved as sensitivity branches tied to stress ID.
4. Add `stressTest({sourceRevisionId,assumption})` and `repairStress({stressId,constraints})` tool behavior via shared services; ordinary search already accepts assumption. A repair request observes failure, searches, simulates, compares and saves a candidate, or explains verified infeasibility. Do not return an un-stressed high Score as proof of repair.
5. Prompt module: identify exact assumption and user goal; read original and stressed evaluations; separate construction cost/lag from broad municipal predictions; preserve source/locks; only claim robustness to the tested assumption. A successful stress test is not a probability estimate. Explain sacrifices and carry the selected variant into later brief.
6. Allow apply/fork of repaired decision set to current scenario with visible source: ordinary official evaluation is recalculated from original dataset, while attached experiment retains its sensitivity result. Never silently replace original catalogue cost/lag or combine these in team ranking. The user can continue editing either decisions or assumptions.
7. Persist repeated experiment versions or new IDs; changing source marks prior stress historical. AI interrupt/retry reuses receipts and leaves useful saved evaluations; failure in repair keeps original experiment.
Acceptance: V5 on constrained two-district portfolio (cost100 with M3 cost30) at+20% makes cost106 and no valid sensitivity Score. Search respects pinned M7/Nura and100; return actual legal repair or evidenced none. Delay+1 changes realized M3 effects/Score; delay cap8 no negative realized share. Reopen experiment, switch locale and apply repaired choices with both official/sensitivity provenance intact. Focused math/CAS and one live stress prompt path.
Commit: complete sensitivity/repair. Exit02: full workshop works; update PROJECT feature status/STATE, stop writers and hand off03.1. Export/share/brief UI still absent until implemented, not shown as disabled promised placeholders.
