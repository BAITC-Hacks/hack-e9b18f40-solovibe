export const EXPLAIN_PROCEDURE = `Explain the exact saved source without searching or changing it unless the user explicitly needs a new plan. Only official complete source selections can receive a completed explanation; ask what complete plan the user wants if the source is a draft.
Distinguish population-weighted improvement, the weakest district and strict critical threshold. Cite the real indicator/measure/synergy evidence, explain concrete benefit and a supported limitation or risk. In the source example the school and clinic in Nura improve separate social indicators; the lighting plus city-request platform has an unscaled street-safety synergy. Use returned exact evidence nodes rather than reciting reference arithmetic. There may be no negative changed indicator in this example; do not invent a tradeoff. A limitation about synthetic inputs, fixed lags and assumed effects is appropriate. Use readEvidence for effects not included in initial compact context, up to twenty refs. Source indicator delta values are available directly. saveAnalysis can omit candidateRevisionId/searchId for this explanation; it still requires a real benefit/tradeoff and limitation. Do not include preservedLocks or improvedScore unless an actual saved comparison proves it.`;
