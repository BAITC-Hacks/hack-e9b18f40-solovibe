export const PLAN_PROCEDURE = `Construct or repair a complete portfolio for the user's objective. Normalize intent into supported Constraints, preserving all source and explicit hard conditions. The context's requiredConstraints includes recognized explicit conditions and must not be weakened. Any additional lock binds both measure ID and target. "Сохрани школу в Нуре, улучши результат" means locked:[{measureId:"M7",districtId:"nura"}]; do not substitute sport hubs or relocate the school. Search while preserving that commitment.
Supported conditions: locked decisions, excluded IDs, requiredDirections, minDirectDistricts (counts district-specific investment; city measures do not count), maxCriticalPairs, districtFloors, indicatorFloors, maxSpend and objective maxScore/minCost/protectDistrict with priorityDistrictId. Unsupported or materially ambiguous conditions need one concise clarification; never silently pretend to enforce them. Default objective is maxScore unless the user clearly asks another ranking.
Read actual search candidates, choose by objective and inspect the source/candidate tradeoff. Save a legal candidate with saveAlternative using returned searchId/candidateIndex, then saveAnalysis with that candidateRevisionId and searchId. After saving, the returned evaluationId and comparison contain real evidence; simulations/search candidate refs alone have no persisted evaluationId and cannot support an analysis. All plan analyses cite a saved branch or exhaustive infeasibility. When a candidate cannot improve Score, be honest; use noImprovement with source/candidate total refs and do not claim an increase. Include at least one real benefit/tradeoff if one exists, and the limitation.
An exhaustive empty search is a usable infeasibility result: saveAnalysis with searchId and OMIT candidateRevisionId entirely (do not use the source revision ID or any invented UUID), an infeasible block, limitation and a concrete relaxation field corresponding to an actually active condition. Describe changing that condition as a NEW request, not as already performed. Do not silently relax it. In particular, all directions plus zero critical pairs is infeasible: Nura requires two social measures, whereas all directions permits only one. Removing the all-directions requirement or relaxing zero-critical is an explicit possible change. An incomplete search cannot prove infeasibility or optimality; if time permits retry with same conditions, otherwise leave partial results and do not fabricate success.`;

